name: Atualizar comparativo INCC e legibilidade

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/add-incc-salary-history.yml'
      - 'scripts/add-incc-salary-history.py'
      - 'scripts/refine-incc-table.py'
      - 'scripts/improve-readability.py'
      - 'scripts/add-floating-navigation.py'
      - 'scripts/add-contemplation-credit.py'
      - 'scripts/remove-aportes-menu.py'
      - 'scripts/add-section-focus.py'
      - 'scripts/restore-simulation-typography.py'
      - 'scripts/restore-contemplation-month-year.py'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: github-pages-incc
  cancel-in-progress: true

jobs:
  patch:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Atualizar comparativo para cliente e mobile
        run: python scripts/add-incc-salary-history.py

      - name: Refinar design da tabela e mostrar quem subiu mais
        run: python scripts/refine-incc-table.py

      - name: Aumentar legibilidade geral e reduzir microtexto
        run: python scripts/improve-readability.py

      - name: Adicionar menu flutuante de navegação
        run: python scripts/add-floating-navigation.py

      - name: Adicionar ajuste de crédito na contemplação
        run: python scripts/add-contemplation-credit.py

      - name: Remover Aportes do menu lateral
        run: python scripts/remove-aportes-menu.py

      - name: Adicionar foco automático por seção
        run: python scripts/add-section-focus.py

      - name: Restaurar tipografia original da simulação
        run: python scripts/restore-simulation-typography.py

      - name: Restaurar seleção Mês/Ano da contemplação
        run: python scripts/restore-contemplation-month-year.py

      - name: Salvar alteração
        shell: bash
        run: |
          if git diff --quiet; then
            echo "Nenhuma alteração de conteúdo necessária."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add calculadora/index.html calculadora/service-worker.js
            git commit -m "Restaurar seleção de mês e ano da contemplação"
            git push
          fi

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Preparar arquivos do site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Publicar versão atualizada
        id: deployment
        uses: actions/deploy-pages@v4
